rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All access is via backend Admin SDK — deny all client-side access
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
